---
kind: Service
apiVersion: v1
metadata:
  name: controller
  namespace: unifi
spec:
  selector:
    name: controller
  type: NodePort
  ports:
    - targetPort: https
      port: 443
      name: https
    - targetPort: metrics
      port: 9130
      name: metrics
